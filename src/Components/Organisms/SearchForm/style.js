import styled from "styled-components";

export const Search = styled.div`
  flex: 1;

  > form {
    display: flex;
    place-items: center;
    gap: 8px;
    flex: 1;

    > button {
      aspect-ratio: 1;
    }
  }
`;
